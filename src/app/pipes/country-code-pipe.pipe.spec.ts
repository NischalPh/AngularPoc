import { CountryCodePipePipe } from './country-code-pipe.pipe';

describe('CountryCodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountryCodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
